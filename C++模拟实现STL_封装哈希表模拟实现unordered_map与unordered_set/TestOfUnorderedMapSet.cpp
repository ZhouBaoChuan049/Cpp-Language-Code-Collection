//需求;
//一个哈希桶为底层封装出两个容器
//enum State，HashData，HashFunc,string特化
//__stl_next_prime，HashNode
//HTIterator：operator*()，operator->()，operator!=，operator==，operator++()
//HashTable：Begin()，End()，const//，构造和析构，Insert，Find，Erase
//unordered map: MapKeyOfT,begin(),end(), operator[],insert,Find,Erase
//测试：待会儿再说。
#include"MyUnorderedMap.h"
int main()
{
	MyUnorderedMap::test_map1();
	return 0;
}